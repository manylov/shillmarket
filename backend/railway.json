{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "RAILPACK",
    "buildCommand": "npx prisma generate && npx tsc"
  },
  "deploy": {
    "startCommand": "npx prisma migrate deploy 2>&1 || true; node dist/index.js"
  }
}
